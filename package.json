{
  "scripts": {
    "deploy:cloudflare": "wrangler deploy cloudflare/worker.js",
    "dev:cloudflare": "wrangler dev cloudflare/worker.js --local"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
